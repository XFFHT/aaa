OPENAI_BASE_URL=https://your-openai-compatible-endpoint/v1
OPENAI_API_KEY=replace_with_real_key
OPENAI_MODEL=gpt-5.5

WORKER_TOKEN=replace_with_random_worker_token
WORK_DIR=D:\HermesRAG\paper-asset\work
OUTPUT_DIR=D:\HermesRAG\paper-asset\outputs

# Default is 0 so requests ignore Windows/Clash system proxy.
# Set to 1 only if the endpoint must be accessed through system proxy.
LLM_TRUST_ENV_PROXY=0
